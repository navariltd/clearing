[project]
name = "clearing"
authors = [
    { name = "Nelson Mpanju", email = "nelsonnorbert87@gmail.com"}
]
description = "Clearing and Forwarding"
requires-python = ">=3.14"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # Managed by bench, pinned here for development/migrations alignment
    "frappe~=16.0.0-dev"
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"
